name: CI/CD Pipeline

on:
  push:
    branches:
      - main  # Trigger workflow on pushes to the main branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout Code
      - name: Checkout Code
        uses: actions/checkout@v2

      # Step 2: Set up Docker Buildx (for cross-platform image building)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      # Step 3: Log in to Amazon ECR
      - name: Log in to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1
        with:
          region: us-east-2  # Specify your AWS region

      # Step 4: Build and Push Backend Image
      - name: Build and Push Backend Docker Image
        run: |
          docker build -t ${{ secrets.ECR_BACKEND }}:latest ./backend
          docker push ${{ secrets.ECR_BACKEND }}:latest
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-2

      # Step 5: Build and Push Frontend Image
      - name: Build and Push Frontend Docker Image
        run: |
          docker build -t ${{ secrets.ECR_FRONTEND }}:latest ./frontend
          docker push ${{ secrets.ECR_FRONTEND }}:latest
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-2

      # Step 6: Configure AWS CLI (for interacting with EKS)
      - name: Configure AWS CLI
        run: |
          aws eks --region us-east-2 update-kubeconfig --name eks  
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-2

      # Step 7: Deploy Backend Service and Pods to EKS
      - name: Apply Backend Kubernetes Config
        run: |
          kubectl apply -f k8s/backend-deployment.yaml
        env:
          KUBECONFIG: ${{ secrets.KUBECONFIG }}  # Ensure this secret is correctly set

      # Step 8: Deploy Frontend Service and Pods to EKS
      - name: Apply Frontend Kubernetes Config
        run: |
          kubectl apply -f k8s/frontend-deployment.yaml
        env:
          KUBECONFIG: ${{ secrets.KUBECONFIG }}  # Ensure this secret is correctly set
